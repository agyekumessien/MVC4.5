@model IEnumerable<MainProgram.Models.tbh_RepliesVM>

<h2 class="text-success text-center">Welcome to ChartRoom!</h2>

@*<p>
        @Html.ActionLink("Create New", "Create")
    </p>*@
<table class="table text-center;" style="width:60%; margin-left:20%; text-align:center">
    @*  <tr>
            <th>
                @Html.DisplayNameFor(model => model.User.UserName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Text)
            </th>
            <th></th>
        </tr>*@

    @foreach (var postreply in Model)
    {

        <tr style="border:1px solid black">
            @*<td>
                    @Html.DisplayFor(modelItem => comment.User.UserName)
                </td>*@





            <td>
                <span style="margin-right:15px; font-size:16px; color:green">
                    @postreply.AspNetUser.UserName
                </span>

                <span style="font-size:16px; margin-right:15px;">
                    @Html.DisplayFor(modelItem => postreply.ReplyText)
                </span>

                <span>
                    @postreply.CreatedOn
                </span>
                @foreach (var postReply in postreply.tbh_Replies1)
                {
                    <br />

                    <span style="margin-right:15px">
                        @postreply.AspNetUser.UserName
                    </span>
                    <span style="margin-right:15px; font-size:16px; color:green">
                        @postreply.ReplyText
                    </span>
                    <span style="margin-right:15px; font-size:16px; color:green">
                        @postreply.CreatedOn
                    </span>
                    <br /><br />
                    using (Html.BeginForm("PostReply", "ChatRoom, FormMethod.Post"))
                    {
                        <input type="text" name="Reply" style="width:60%; height:100px; font-size:16px; margin-bottom:10px">
                        <br />
                        <input type="hidden" name="CID" value="@postreply.ReplyId" />
                        <input type="submit" value="post  reply" class="btn btn-default"
                               styles="margin-top:10px">
                    }
                }

            </td>




            @* <td>
                    @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
                    @Html.ActionLink("Details", "Details", new { id=item.Id }) |
                    @Html.ActionLink("Delete", "Delete", new { id=item.Id })
                </td>*@
        </tr>
    }

</table>


